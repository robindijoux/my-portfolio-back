meta {
  name: List technologies
  type: http
  seq: 2
}

get {
  url: {{base_url}}/technologies
  body: none
  auth: inherit
}

tests {
  test("should get technologies list successfully", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody()).to.be.an('array');
  });
}

docs {
  Get all available technologies.
  
  Returns an array of technology objects with:
  - id: Unique identifier
  - technology: Technology name
  - iconUrl: Technology icon URL
}
